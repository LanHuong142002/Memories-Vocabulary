// Button
export enum BUTTON_TYPE {
  SUBMIT = 'submit',
  BUTTON = 'button',
}

// Topic
export enum TOPIC_VARIANT {
  DEFAULT = 'default',
  SELECTED = 'selected',
}

// Typography
export enum TYPOGRAPHY_TEXT_ALIGN {
  CENTER = 'center',
  RIGHT = 'right',
  LEFT = 'left',
}

export enum TYPOGRAPHY_TAG_NAME {
  P = 'p',
  SPAN = 'span',
}

// Label
export enum LABEL_COLOR {
  SUCCESS = 'success',
  FAILED = 'failed',
  NORMAL = 'normal',
}

export enum SIZE {
  XXS = 'xxs',
  XS = 'xs',
  S = 's',
  M = 'm',
  L = 'l',
  XL = 'xl',
  XXL = 'xxl',
  XXXL = 'xxxl',
}

export enum VARIANT {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  DEFAULT = 'default',
  SELECTED = 'selected',
}
